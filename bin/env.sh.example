export JDK_JAVA_OPTIONS="-Xmx16G"

# An TTS_VENV like the one commented out is useful for development purposes to run the setup-servers tools from
#   source code. Otherwise, the scripts in this directory will take care of setting up
#   the virtual environment and you can skip pointing to a virtual environment directory
#TTS_VENV=/home/essaids/venvs/clickactions/venv

TTS_HAPI_PORT=8888
TTS_FHIR_ADDRESS="http://localhost:{}/fhir"
TTS_VENV=


